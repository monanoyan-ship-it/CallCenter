@{
    ViewData["Title"] = "Salon Profili";
}

<div id="profile-vm">
    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white fw-semibold"><i class="bi bi-shop me-2"></i>Firma Bilgileri</div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-12">
                            <label class="form-label small">Aciklama</label>
                            <textarea class="form-control form-control-sm" rows="3" data-bind="value: form.description" placeholder="Salonunuz hakkinda kisa bilgi..."></textarea>
                        </div>
                        <div class="col-6">
                            <label class="form-label small">Web Sitesi</label>
                            <input type="url" class="form-control form-control-sm" data-bind="value: form.website" />
                        </div>
                        <div class="col-6">
                            <label class="form-label small">Durum</label>
                            <select class="form-select form-select-sm" data-bind="value: form.isPublished">
                                <option value="true">Yayinda</option>
                                <option value="false">Taslak</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card border-0 shadow-sm mt-3">
                <div class="card-header bg-white fw-semibold"><i class="bi bi-globe me-2"></i>Sosyal Medya</div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-6">
                            <label class="form-label small"><i class="bi bi-instagram me-1"></i>Instagram</label>
                            <input type="text" class="form-control form-control-sm" placeholder="@@salonadi" data-bind="value: form.instagramHandle" />
                        </div>
                        <div class="col-6">
                            <label class="form-label small"><i class="bi bi-facebook me-1"></i>Facebook</label>
                            <input type="url" class="form-control form-control-sm" data-bind="value: form.facebookUrl" />
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white fw-semibold"><i class="bi bi-receipt me-2"></i>Faturalama</div>
                <div class="card-body">
                    <label class="form-label small">Faturalama Tercihi</label>
                    <div class="form-check mb-2">
                        <input class="form-check-input" type="radio" name="billingType" id="bt1" value="1" data-bind="checked: form.billingType" />
                        <label class="form-check-label small" for="bt1">
                            <strong>Tek Fatura</strong><br />
                            <span class="text-muted">Tum subeler icin merkez sube fatura bilgileri kullanilir</span>
                        </label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="billingType" id="bt2" value="2" data-bind="checked: form.billingType" />
                        <label class="form-check-label small" for="bt2">
                            <strong>Sube Bazli</strong><br />
                            <span class="text-muted">Her sube kendi fatura bilgilerini kullanir</span>
                        </label>
                    </div>
                </div>
            </div>

            <div class="card border-0 shadow-sm mt-3">
                <div class="card-body text-center text-muted small">
                    <i class="bi bi-info-circle me-1"></i>Adres, telefon, calisma saatleri ve fatura bilgileri
                    <a href="/Branches"><strong>Subeler</strong></a> sayfasindan yonetilir.
                </div>
            </div>

            <button class="btn btn-primary w-100 mt-3" data-bind="click: save, disable: isSaving">
                <span data-bind="visible: isSaving" class="spinner-border spinner-border-sm me-1"></span>
                <i class="bi bi-check-lg me-1" data-bind="visible: !isSaving()"></i>Profili Kaydet
            </button>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/Profile.js" asp-append-version="true"></script>
}

@inject CallCenter.Shared.Services.ITranslationService Translation
@{
    ViewData["Title"] = Translation.T("salon.account.reset.title", "Şifre Sıfırlama");
    Layout = null;
    var token = ViewBag.Token as string ?? "";
    var error = ViewBag.Error as string;
    var success = ViewBag.Success != null && (bool)ViewBag.Success;
    var message = ViewBag.Message as string ?? "";
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><t key="salon.account.reset.page_title">CorpLynk Salon - Şifre Sıfırlama</t></title>
    <link href="/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/lib/bootstrap-icons/font/bootstrap-icons.min.css" rel="stylesheet" />
    <style>
        body { background: linear-gradient(135deg, #2d1b3d 0%, #1a1028 100%); min-height: 100vh; display: flex; align-items: center; justify-content: center; }
        .login-card { background: #fff; border-radius: 12px; padding: 2.5rem; width: 100%; max-width: 420px; box-shadow: 0 8px 32px rgba(0,0,0,.2); }
        .brand { color: #2d1b3d; font-weight: 700; font-size: 1.5rem; }
        .brand span { color: #ce93d8; }
        .btn-salon { background: #7b1fa2; border: none; color: #fff; }
        .btn-salon:hover { background: #6a1b9a; color: #fff; }
    </style>
</head>
<body>
    <div class="login-card">
        <div class="text-center mb-4">
            <div class="brand"><i class="bi bi-shield-lock me-2"></i><t key="salon.account.reset.heading">Yeni Şifre Belirle</t></div>
        </div>

        @if (success)
        {
            <div class="alert alert-success py-2 small">@message</div>
            <a href="/Account/Login" class="btn btn-salon w-100"><t key="salon.account.reset.go_login">Giriş Yap</t></a>
        }
        else
        {
            @if (!string.IsNullOrEmpty(error))
            {
                <div class="alert alert-danger py-2 small">@error</div>
            }
            @if (!string.IsNullOrWhiteSpace(token))
            {
                <form method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="token" value="@token" />
                    <div class="mb-3">
                        <label class="form-label small"><t key="salon.account.reset.new_password">Yeni Şifre</t></label>
                        <input type="password" name="newPassword" class="form-control" minlength="6" required autofocus />
                    </div>
                    <div class="mb-3">
                        <label class="form-label small"><t key="salon.account.reset.confirm_password">Yeni Şifre (Tekrar)</t></label>
                        <input type="password" name="confirmPassword" class="form-control" minlength="6" required />
                    </div>
                    <button type="submit" class="btn btn-salon w-100"><t key="salon.account.reset.submit">Şifreyi Güncelle</t></button>
                </form>
            }
            else
            {
                <a href="/Account/ForgotPassword" class="btn btn-salon w-100"><t key="salon.account.reset.request_new">Yeni Bağlantı İste</t></a>
            }
        }
    </div>
</body>
</html>
